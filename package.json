{
  "name": "sakthi-ai-nexus",
  "version": "0.11.0-owner-security",
  "private": true,
  "description": "Free-first SakthiAI owner platform with local privacy lock, encrypted backups, guarded tools, projects, artifacts, memory and usage controls.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test": "npm run validate && npm run build && npm run test:unit"
  },
  "engines": {"node": ">=20"}
}
