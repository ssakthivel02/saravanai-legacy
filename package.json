{
  "name": "sakthi-ai-nexus",
  "version": "0.3.1",
  "private": true,
  "description": "Release 003.1: free-first SakthiAI routing, streaming and cited public-data research with optional premium providers.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test": "npm run validate && npm run build && npm run test:unit"
  },
  "engines": {"node": ">=20"}
}
