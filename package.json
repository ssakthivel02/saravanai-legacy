{
  "name": "sakthi-ai-nexus",
  "version": "0.11.0",
  "private": true,
  "description": "SakthiAI zero-cost owner platform with paid-provider hard lock, JSON health, projects, artifacts, approvals, memory and usage controls.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test": "npm run validate && npm run build && npm run test:unit"
  },
  "engines": {"node": ">=20"}
}
