{
  "name": "sakthi-ai-nexus",
  "version": "0.3.0",
  "private": true,
  "description": "Release 003: multi-provider SakthiAI workspace with streaming and fresh cited web research.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test": "npm run validate && npm run build && npm run test:unit"
  },
  "engines": {"node": ">=20"}
}
