{
  "name": "sakthi-ai-nexus",
  "version": "0.10.0-owner.0",
  "private": true,
  "description": "Free-first SakthiAI owner platform preview with projects, artifacts, approvals, memory, usage controls and guarded mobile APIs.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test": "npm run validate && npm run build && npm run test:unit"
  },
  "engines": {"node": ">=20"}
}
