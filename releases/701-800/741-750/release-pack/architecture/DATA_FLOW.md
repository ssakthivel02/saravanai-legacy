# Data flow

Request → identity and tenant resolution → schema validation → policy decision →
approval, accessibility and budget verification → bounded handling →
privacy-safe audit metadata → evidence references → response or correction path.
