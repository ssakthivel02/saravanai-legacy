# Low-level design

Each release provides a TypeScript contract, policy evaluator, service,
status route, privacy-safe telemetry contract, strict JSON Schema,
tenant-scoped D1 design, OpenAPI metadata, tests and rollback guidance.
