# Data flow

Request → trusted identity/tenant resolution → schema validation → policy decision
→ approval verification where required → domain handling → privacy-safe audit
metadata → evidence references → response.
