# Backup and restore

Record source commit, schema version, timestamp, encryption method, checksum and custodian.
A backup is valid only after restoration into an isolated non-production database and
verification of row counts, tenant boundaries and critical queries.
