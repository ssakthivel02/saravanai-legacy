# Rollback

Stop rollout; preserve request IDs and evidence; disable the affected flag or route;
revert to the last validated commit; restore the tested D1 backup where required;
validate identity, tenant isolation, health and core journeys; record recovery evidence.
