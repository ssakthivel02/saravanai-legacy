export interface DomainEvent<T=Record<string,unknown>>{eventId:string;tenantId:string;type:string;schemaVersion:number;occurredAt:string;producer:string;correlationId:string;causationId?:string;payload:T;}export function validateEvent(e:DomainEvent):string[]{const r:string[]=[];if(!e.tenantId)r.push("tenant_required");if(e.schemaVersion<1)r.push("schema_version_required");if(!e.correlationId)r.push("correlation_required");return r;}