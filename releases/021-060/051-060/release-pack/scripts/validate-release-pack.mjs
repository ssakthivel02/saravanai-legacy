import{stat,readdir,readFile}from"node:fs/promises";import{join,relative}from"node:path";const root=new URL("..",import.meta.url).pathname;const required=["src/release051/events.ts", "src/release051/dead-letter.ts", "src/release052/retrieval.ts", "src/release052/ranking.ts", "src/release053/stewardship.ts", "src/release053/deduplication.ts", "src/release054/agent-evaluation.ts", "src/release054/simulation.ts", "src/release055/artifact-generation.ts", "src/release055/integrity.ts", "src/release056/pwa.ts", "src/release056/device-session.ts", "src/release057/collaboration.ts", "src/release057/notifications.ts", "src/release058/privacy-rights.ts", "src/release058/records.ts", "src/release059/incident-command.ts", "src/release059/chaos.ts", "src/release060/assurance-board.ts", "src/release060/closure.ts", "src/index.ts", "policies/safety-defaults.json", "policies/pwa-cache-policy.json", "openapi/releases-051-060.yaml", "evidence/GO_NO_GO_TEMPLATE.md", "evidence/PROGRAMME_CLOSURE_TEMPLATE.md"];for(const p of required){try{if(!(await stat(join(root,p))).isFile())throw 0;}catch{throw new Error(`Missing required file: ${p}`);}}async function walk(d){let o=[];for(const e of await readdir(d,{withFileTypes:true})){const p=join(d,e.name);o.push(...(e.isDirectory()?await walk(p):[p]));}return o;}const files=await walk(root);for(const f of files){const t=await readFile(f,"utf8").catch(()=>null);if(t&&/-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----/.test(t))throw new Error(`Secret-shaped content in ${relative(root,f)}`);}console.log(`Release pack validation passed: ${files.length} files`);