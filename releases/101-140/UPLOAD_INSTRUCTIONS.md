# Upload instructions

1. Switch GitHub Desktop to `main`, Fetch origin and Pull origin after PR #9 merge.
2. Create `feature/releases-101-140-enterprise-transformation`.
3. Copy this ZIP's `.github` and `releases` folders into the repository root.
4. Keep inner `release-pack` directories under `releases/101-140/<range>/`.
5. Commit, push and create a draft pull request.
6. Merge only after all pack and SakthiAI checks pass.
