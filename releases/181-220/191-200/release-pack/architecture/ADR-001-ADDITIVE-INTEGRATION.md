# ADR-001: Additive integration

Keep release packs under `releases/181-220/` until a separate implementation PR
wires selected modules into the live Worker. This avoids overwriting the production baseline.
