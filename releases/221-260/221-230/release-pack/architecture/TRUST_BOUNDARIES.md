# Trust boundaries

Browser input, telemetry, model output, assets, suppliers, cloud APIs and generated artefacts are untrusted until validated. Identity, tenant scope and approvals are server-derived.