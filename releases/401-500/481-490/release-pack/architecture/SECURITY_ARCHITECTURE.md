# Security architecture

Default deny, phishing-resistant authentication for privileged actions,
least privilege, scoped service identities, secret rotation, input validation,
outbound allowlists, audit metadata and tested incident response are required.
