# Data flow

Request → trusted identity and tenant resolution → schema validation →
policy decision → approval verification → domain handling → privacy-safe
audit metadata → evidence references → response.
