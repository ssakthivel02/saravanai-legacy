# High-level design — Releases 591–600

The pack is an additive Enterprise Platform v5 domain layer. The existing
Worker remains the authenticated API entry point. Identity, tenant, policy,
approval, audit, appeal and evidence services are mandatory shared controls.
