# Security architecture

Default deny, phishing-resistant privileged access, least privilege, scoped
identities, secret rotation, input validation, egress allowlists, dependency
provenance, audit metadata and tested incident response are required.
