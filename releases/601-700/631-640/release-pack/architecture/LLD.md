# Low-level design

Each release provides a TypeScript contract, policy evaluator, service,
disabled-by-default status route, strict JSON Schema, tenant-scoped D1 design,
OpenAPI metadata, unit tests, documentation and rollback guidance.
