# Data flow

Request → identity and tenant resolution → schema validation → policy decision →
approval and budget verification → bounded domain handling → privacy-safe audit
metadata → evidence references → response.
