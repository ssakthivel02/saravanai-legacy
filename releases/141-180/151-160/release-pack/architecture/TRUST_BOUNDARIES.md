# Trust boundaries

Browser input, user preferences, notifications, uploaded data, model output, plugins,
suppliers and generated artefacts are untrusted until validated. Identity, tenant scope,
approvals, entitlements and production permissions must be server-derived.
