# Runtime Wave 3 High-Level Design

Client
→ Cloudflare Access owner boundary
→ Wave 3 enable gate
→ emergency-stop gate
→ deterministic knowledge/research control
→ metadata-only response

The runtime is deliberately disconnected from web fetch, D1, vector storage,
Workers AI, paid providers and publication systems.
